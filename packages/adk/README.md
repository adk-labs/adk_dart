# adk

`adk` is a facade package that re-exports `adk_dart`.

Use this package when you want the short package name:

```dart
import 'package:adk/adk.dart';
```

The CLI executable is also exposed as `adk`.
