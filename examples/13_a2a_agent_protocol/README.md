# 13. Agent-to-Agent (A2A) Protocol

A2A 표준 프로토콜을 사용하여 AgentCard 생성, 태스크 실행(`A2aExecutor`), 이벤트 스트리밍(`A2aEventQueue`)을 시연하는 예제입니다.

## 실행 방법

```bash
cd examples/13_a2a_agent_protocol
dart pub get
dart run bin/main.dart
```
