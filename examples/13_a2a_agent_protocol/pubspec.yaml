name: a2a_agent_protocol_example
description: An example demonstrating Agent-to-Agent (A2A) protocol communication, agent cards, and task execution.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.11.0

dependencies:
  adk_dart:
    path: ../../

dev_dependencies:
  lints: ^6.0.0
