# 11. Human-In-The-Loop User Choice

`GetUserChoiceTool`을 활용하여 사용자에게 여러 선택지를 제시하고 확인/선택받아 다음 단계를 진행하는 에이전트 예제입니다.

## 실행 방법

```bash
cd examples/11_hitl_user_choice
dart pub get
export GEMINI_API_KEY="your-gemini-api-key"
dart run bin/main.dart
```
