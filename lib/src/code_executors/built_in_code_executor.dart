/// Built-in default code executor aliases.
library;

import 'unsafe_local_code_executor.dart';

/// Built-in executor alias used by ADK defaults.
typedef BuiltInCodeExecutor = UnsafeLocalCodeExecutor;
